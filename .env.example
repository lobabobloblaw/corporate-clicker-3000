# Discord Application Configuration
# Get these from: https://discord.com/developers/applications

# Discord OAuth2 Client ID (REQUIRED)
# This is safe to expose in client-side code
VITE_DISCORD_CLIENT_ID=your_client_id_here

# Discord OAuth2 Client Secret (REQUIRED)
# CRITICAL: Keep this secret! Never commit to git or expose to client
# Only used server-side for token exchange
DISCORD_CLIENT_SECRET=your_client_secret_here

# OAuth2 Redirect URI (REQUIRED)
# Must match exactly what's configured in Discord Developer Portal
# For local development: http://localhost:5173
# For production: https://your-domain.com
REDIRECT_URI=http://localhost:5173

# Cloudflare Tunnel URL (OPTIONAL - for local HTTPS testing)
# Used when testing in actual Discord client during development
# Example: https://your-tunnel-name.trycloudflare.com
# CLOUDFLARE_TUNNEL_URL=

# Environment (OPTIONAL)
# Options: development, production
NODE_ENV=development

# Server Port (OPTIONAL)
# Default: 5173
# PORT=5173

# Session Secret (REQUIRED for production)
# Used to encrypt session cookies
# Generate with: openssl rand -base64 32
# SESSION_SECRET=your_session_secret_here

# Redis Configuration (OPTIONAL - for production session storage)
# If not set, uses in-memory session storage (dev only)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=

# Database Configuration (OPTIONAL - if you need persistent storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Logging Level (OPTIONAL)
# Options: debug, info, warn, error
# LOG_LEVEL=info

# CORS Origins (OPTIONAL - for production)
# Comma-separated list of allowed origins
# CORS_ORIGINS=https://discord.com,https://canary.discord.com,https://ptb.discord.com

# ===================================
# IMPORTANT SECURITY NOTES:
# ===================================
# 1. Copy this file to .env and fill in your values
# 2. NEVER commit .env to git (it's in .gitignore)
# 3. NEVER expose DISCORD_CLIENT_SECRET in client code
# 4. Use different credentials for dev and production
# 5. Rotate secrets regularly in production
# 6. Use environment variables in your hosting platform
# ===================================
